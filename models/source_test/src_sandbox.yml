version: 2
sources:
  - name: sandbox_source
    database: mike_fuller_sandbox
    schema: "{{ env_var('DBT_SOURCE_SCHEMA') if env_var('DBT_SOURCE_SCHEMA') != 'default' else 'source_a' }}"
    tables:
      - name: test_table